# Schemas Module

## Responsibilities
This module defines Pydantic models for data validation and serialization.

## Files
- `requests.py`: Contains request models like `ScanRequest` for the `/scan` endpoint.
